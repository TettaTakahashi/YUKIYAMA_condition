<div class="container">  
  
  <h2 class="mt-5">スキー場経営者　ログイン</h2>
  
  <h5 class="mt-3">会員の方はこちらから</h5>
  
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  
  <table cellpadding="10", class="mt-3", style="font-size: 20px;">
    
    <tr>
      <th>
        メールアドレス
      </th>
      <td>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com" %>
      </td>
    </tr>
  
    <tr>
      <th>
        パスワード
      </th>
      <td>
        <%= f.password_field :password, autocomplete: "current-password" %>
      </td>
    </tr>
    
    <tr><th></th><td><%= f.submit "ログイン", class:"btn btn-primary" %></td></tr>
    
    </table>
  <% end %>
  
  <h4 class="mt-5">
    会員登録がお済みでない方は<br>
    <%= link_to "こちら", new_owner_registration_path %>から新規登録を行なってください。
  </h4>

</div>
